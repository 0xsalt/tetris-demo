{
  "name": "graybeard-demo",
  "version": "0.1.0-alpha",
  "description": "TypeScript demo test software",
  "type": "module",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist",
    "dev": "bun run --watch src/index.ts",
    "test": "bun test",
    "lint": "bunx tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest"
  }
}
